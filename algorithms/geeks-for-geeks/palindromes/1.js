(() => {
  let input = [3,3,2,1,3];
  equalizeArray(input);
})();

function equalizeArray(arr) {
  let hashMap = [];
  for (let i = 0; i < arr.length; i++)
    arr[i] in dictionary ? dictionary[arr[i]] += 1 : dictionary[arr[i]] = 1;

  dictionary
  let max = Math.max(...Object.keys(dictionary));
  let other = Object.keys(dictionary).filter((key) => Number(key) !== max);
  // Save the occurences of each numbers.
  // count all the number occurences that are not the max.
  let matrix = new Array(arr.length).fill(0);
  for (let i = 0;)
}

function hash(val) {
  for (let i = 0; i < String())
}

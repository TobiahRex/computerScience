/*
  Problem Description:

*/


(() => {
  const answers = [
    {
      x: 'x',
    }
  ].map(getAnswer)
  console.log('answers: ', answers);
})();

function getAnswer(input) {
  // TODO
  return -1;
}
